apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: egd-counter
  namespace: openshift-gitops
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  destination:
    namespace: egd-counter
    server: https://kubernetes.default.svc
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
  source:
    repoURL: https://github.com/sika-training-examples/2026-02-05_counter_egd_helm_example.git
    path: .
    helm:
      valuesObject:
        host: egd-counter.apps.egd.germanywestcentral.aroapp.io
        hostExp: egd-counter-exp.apps.egd.germanywestcentral.aroapp.io

        EXTRA_TEXT: 'EGD'
        FONT_COLOR: red
        BACKGROUND_COLOR: yellow

        backendReplicas: 1
        frontendReplicas: 1
        frontendExpReplicas: 1

        ingressTlsEnabled: false

        enableArgoCDHooks: true
        enableServiceMonitor: true
        openshiftDashboardEnabled: true
        openshiftConsoleLinkEnabled: true
        grafanaDashboardEnabled: true
